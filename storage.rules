rules_version = '2';

// Placeholder — proper rules in Phase 3 (image upload).
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
